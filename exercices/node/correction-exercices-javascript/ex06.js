const add = (nb1, nb2) => {
  return nb1 + nb2
}

const sub = (nb1, nb2) => {
  return nb1 - nb2
}

const mul = (nb1, nb2) => {
  return nb1 * nb2
}

const div = (nb1, nb2) => {
  return nb1 / nb2
}

console.log(add(10, 11))
console.log(sub(6, 4))
console.log(mul(4, 5))
console.log(div(10, 2))
